# Deprecated: Contract Inventory

This repository is not the active shared-contract inventory or promotion office.

Active Project Gate-owned contracts are package envelopes only: `stage-bundle.v1`, `target-input.v1`, and `transition-result.v1`.

Specialist repositories remain responsible for their domain schemas and validators.
