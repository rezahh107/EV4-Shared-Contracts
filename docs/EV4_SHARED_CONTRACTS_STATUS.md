# Deprecated: EV4 Shared Contracts Status

This document is retained only as historical context from the earlier shared-contract skeleton role.

Active repository status now lives in:

```text
docs/EV4_PROJECT_GATE_STATUS.md
```

Active role:

```text
EV4 stage output → Project Gate validates evidence → deterministic transition → next-stage JSON input package
```

Do not treat this repository as the active shared-contract promotion office. The active implementation is the Python package under `src/ev4_transition`.
