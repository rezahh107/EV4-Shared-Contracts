# Deprecated: Compatibility Map

This document previously supported the shared-contract coordination role.

Active compatibility decisions now happen through concrete Project Gate transitions:

```text
source stage bundle → transition definition → target input package
```

Use `transitions/*.json`, `schemas/*`, and tests as the active compatibility evidence.
