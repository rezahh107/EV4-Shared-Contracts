# Deprecated: Promotion Rules

The previous shared-contract promotion model is no longer active architecture.

Project Gate changes should now be reviewed against deterministic transition behavior, schema and fixture coverage, provenance and SHA-256 stability, clear `insufficient_evidence` handling, and passing tests.
