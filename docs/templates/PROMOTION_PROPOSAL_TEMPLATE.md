# Deprecated Promotion Proposal Template

This template belonged to the previous shared-contract promotion model.

For active Project Gate changes, use a PR description that states the transition or schema affected, fixtures added, deterministic behavior proven, validation commands run, and known limitations.
